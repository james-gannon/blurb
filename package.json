{
  "name": "blurb-inbox",
  "version": "1.0.0",
  "description": "Blurb Inbox static frontend",
  "scripts": {
    "start": "serve -s public",
    "dev": "concurrently \"nodemon --watch api -e js api-server.js\" \"serve -s public\"",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.39.7"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "serve": "^14.2.3"
  }
}